const state = {
  focusDocument: {},
}

const getters = {
  focusDocument: (state) => state.focusDocument,
}

const actions = {
  xx: ({ commit, state }) => {},
}

const mutations = {
  xx: (state, step) => {},
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
}
